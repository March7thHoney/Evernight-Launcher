// ContentView.swift - Kafka-Launcher
// This file is kept for compatibility. MainView is the primary view.

import SwiftUI

struct ContentView: View {
    var body: some View {
        MainView()
    }
}
